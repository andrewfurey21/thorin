[project]
name = "thorin"
version = "0.1.0"
description = "Compare your compiled code to the assembly using DWARF."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "iced-x86>=1.21.0",
    "pyelftools>=0.32",
]
